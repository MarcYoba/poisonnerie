<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirige de manière invisible vers le sous-dossier public de Symfony
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>